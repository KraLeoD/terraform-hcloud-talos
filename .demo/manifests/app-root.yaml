apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-root
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    path: .demo/manifests/apps
    repoURL: https://github.com/KraLeoD/terraform-hcloud-talos  # UPDATE THIS!
    targetRevision: main  # or master, depending on your branch
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
