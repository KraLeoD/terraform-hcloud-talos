apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "1"  # Deploy after Traefik Helm chart
spec:
  project: default
  source:
    repoURL: https://github.com/KraLeoD/terraform-hcloud-talos
    targetRevision: claude/enhance-cluster-deployment-011CUyitAwQXycAoeb7oEx51
    path: .demo/manifests/apps/traefik
    directory:
      include: "service.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
